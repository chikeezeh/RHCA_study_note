---
- name: Play to create user directory
  hosts: vm1
  tasks:
  - name: create directory
    ansible.builtin.file:
      path: /{{ user_name }}files
      state: directory
      owner: "{{ user_name }}"